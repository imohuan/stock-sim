# 2026-06-04

- **stockApi.ts** — 将硬编码的 westock-data 绝对路径改为 `path.resolve(__dirname, 'westock-data/...')` 相对路径；因 ESM 环境无 `__dirname`，加了 `fileURLToPath(import.meta.url)` polyfill。
- **KlineChart.vue** — 新增 MA30/MA40/MA50/MA60 均线，默认隐藏（legend selected: false），点击图例可像 MA20 一样切换显示。
- **Toolbar.vue** — ① 第 1000 天时总资产可点击编辑；② 新增「清缓存」按钮（重置右侧），调用 `cacheManager.clear()` 清除 localStorage 中 `stock-cache:*` 的 K 线和行情缓存，保留自选股列表（`stock-sim-watchlist`）和数据源设置，然后 `location.reload()` 强制刷新。
- 创建 **README.md**，顶部展示 image.png UI 截图，包含项目简介和快速启动说明。
- **RealDataSource.ts** — 修复真实数据源刷新卡死/不变的 bug。根因：`simDay` 初始值 1000 固定，但 API 拉取的真实 K 线通常不足 1000 条，导致 `manualRefresh()` 的条件 `simDay < maxKlineDays()` 永远为 false，数据永远不变。修复：① `fetchDailyKline` 加载 K 线后调用 `clampSimDay()` 将 simDay clamp 到实际数据量；② `reset()` 使用 `Math.min(SIM_INITIAL_DAYS, maxKlineDays())`；③ `getKline()` 改为 `daily.slice(0, simDay)` 截取，与 MockDataSource 行为一致，实现刷新时渐进添加数据。
- **ChartPanel.vue** — ① 全屏切换按钮；② 全屏时保存当前 `simDay`，切换到 `MOCK_HISTORY_DAYS`(2000) 展示全部 K 线；退出全屏时恢复保存的天数。
- **TradePage.vue** — 新增 `isChartFullscreen` ref，provide 给子组件；全屏时面板全部隐藏。
- **TradeLayout.vue** — 注入 `isChartFullscreen`，左右底容器和分割线加 `hidden`（display:none），中间图表占满视口。
- **market.ts** — 新增 `setSimDay(day)`：直接跳转到指定天数，同步更新 dataSource 和 store。② `initQuoteSubscription` 批量拉取/占位兜底/名称补充（localStorage 缓存 + API fallback）。③ 新增 `initRealMode(codes)`。
- **TradeLogPanel 成交日期改天数** — ① `TradeRecord` 接口新增 `simDay` 字段；② `trading.ts` placeOrder 时从 `marketStore.simDay` 获取当前模拟天数写入记录；③ `TradeLogPanel.vue` 成交日期列改为 `第${r.simDay}天`，不再显示真实日期。后续改为表头「成交日（天）」+ 纯数字显示。
- **交易手续费计算修正** — 之前只算了佣金（万三最低5元），缺少印花税和过户费。修正为 A 股标准：佣金（万三，最低5元）+ 过户费（万分之一，双向）+ 印花税（万分之五，仅卖出）。
- **README.md** — 新增「模拟数据算法」章节（随机漫步模型、OHLC 生成公式、定时器驱动）和「手续费算法」章节（三方费用明细、计算公式、示例表格）。
- **npm 包发布配置** — ① vite.config.ts 新增 npmFontsPlugin：BUILD_MODE=npm 时将 material-symbols/@fontsource 字体本地导入替换为 Google Fonts CDN，包体 5.3M → 921K；② bin/server.mjs CLI 入口（零依赖 HTTP 静态服务器 + API 代理）；③ package.json 加 bin/files/build:npm；④ .npmignore 排除源码。npm pack 后包体 304 kB gzip。
- **npm 缓存机制理解** — npm 按 包名+版本号+integrity hash 缓存；同版本只下载一次；版本更新重新下载完整包，旧版本不自动清理；npx 会缓存到 ~/.npm/_cacache。
- **CLI start/stop/help 命令** — 基于 commander 14.x 实现：① `stock-sim start [--port 5180]` 启动服务，PID 写入 `bin/.stock-sim.pid`，打印 PID 文件绝对路径；② `stock-sim stop` 读取 PID 文件，发 SIGTERM 终止进程并清理文件；③ `stock-sim` 默认显示帮助。边界处理：重复启动检测、stale PID 清理、EADDRINUSE 提示、SIGINT/SIGTERM 优雅退出自动清理 PID 文件。
- **依赖分离** — 将 vue/echarts/tailwindcss 等 18 个运行时/构建依赖全部从 `dependencies` 移至 `devDependencies`，CLI 的 `dependencies` 只保留 `commander`（14.x）。`npx stock-sim` 安装体积从 ~500MB 降至 ~2MB。
- **Git 提交信息修正** — 将最后一个提交的信息从 "1" 改为 "feat: 添加服务端启动脚本，更新构建配置和依赖"，使用 `git commit --amend`。
- **CLI 架构重构：前后端分离编译** — ① bin/server.mjs 从「自己实现 HTTP 服务」改为「纯 CLI 管理脚本」，用 `spawn` 后台启动编译后的 `dist/server/index.js`；② server/index.ts 改为一体化 HTTP 服务（前端静态文件 + 后端 API 代理），通过 `findWestockHome()` 动态检测 westock-data 路径兼容 dev/compiled 两种模式；③ 新增 `scripts/build-server.mjs` 用 esbuild API 编译后端（避免 `bun x` 在 Windows 上解析 `--external:node:*` 失败）；④ build:npm 改为 `vite build --mode npm && node scripts/build-server.mjs`。
- **CLI 架构重构后的问题修复** — ① esbuild 编译后 `import.meta.url` 比较失效导致 server 不启动，移除条件判断直接运行；② stockApiMiddleware 需要剥离 `/api/stock` 前缀，恢复 URL rewrite 逻辑；③ build:npm 从 `BUILD_MODE=npm` 改为 `vite build --mode npm`（利用 Vite 原生 mode 功能，跨平台兼容）；④ westock-data 路径新增 `findWestockHome()` 自动检测两种运行模式。
- **CLI restart 命令** — `stock-sim restart [--port]`：先调 `tryStop()` 停旧服务（已运行则杀进程，未运行则跳过），再 `cmdStart()` 启动。`cmdStop` 重构为 `tryStop()` + `cmdStop()`，前者负责实际停止返回布尔值，后者负责用户交互和 exit。
