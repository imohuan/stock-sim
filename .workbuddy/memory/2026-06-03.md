## 股票模拟交易系统 stock-sim 搭建

- 优化 OrderPanel 交易面板（19:17）：
  - 买卖方向：AxSelect 下拉 → 分段按钮组（买入/卖出双按钮，参考 AxPropPanel segmented 模式）
  - 快捷数量：2手/4手/6手/8手/梭哈/清仓 按钮组，梭哈=买满/卖光，清仓仅卖出时显示
  - 价格逻辑：±10% 涨跌幅限制约束，防止随意低价购买（如 ¥1 买 10 股）
  - 预估明细：展示成交金额、佣金(0.03%/最低5元)、应付/应收总额
  - 购买力提示：资金不足时显示差额
  - 下单按钮带禁用态 + 数量/金额预览
- 头脑风暴确定方案：A股、模拟数据+适配器预留真实接口、标准版功能、单屏全功能布局
- 创建 `packages/stock-sim` 独立 package，bun install 91 依赖（Vue 3 + Pinia + ECharts + vue-echarts + @vueuse/core + dayjs）
- 实现数据层：
  - 100只真实A股快照（stock-list.ts），覆盖银行/保险/证券/白酒/医药/新能源/半导体/家电/汽车/军工/通信/化工等行业
  - MockDataSource：Geometric Brownian Motion 随机游走模拟，每2秒tick更新行情，支持subscribe推送
  - RealDataSource 预留接口（空实现）
  - 4个Pinia stores：settings（数据源/周期/图表切换）、market（行情订阅/推送）、watchlist（自选股增删/持久化localStorage）、trading（账户/下单/持仓/交易记录）
- 实现UI组件（全部使用ax-ui-kit，统一size="sm" rounded="md"，Select min-width 120px）：
  - TradeLayout：CSS Grid 三列两行布局
  - Toolbar：数据源下拉切换 + K线周期 + 分时/K线 + 总资产概览
  - WatchlistPanel + StockRow：自选股列表，添加弹窗（AxDialog + AxSelect搜索）
  - KlineChart：ECharts candlestick + MA5/MA10/MA20 + 成交量 + DataZoom
  - MinuteChart：ECharts 分时图 + 均价线
  - ChartPanel：股票头部信息 + K线/分时切换容器
  - DepthPanel：五档盘口实时刷新
  - OrderPanel：买入/卖出下单，AxSelect/AxInput/AxButton统一规范
  - PositionPanel：持仓列表 + 盈亏实时计算
  - TradeLogPanel：交易记录列表
  - PriceText/ChangeBadge：A股红涨绿跌通用组件
- 所有文件均在 packages/stock-sim 下，通过 @ui 别名引用 ax-ui-kit 组件

## 修复构建报错 (2026-06-03 17:35)

- **notify.error/success 缺失**：`useNotify()` 只暴露了 `triggerNotify`，但 `trading.ts` 和 `OrderPanel.vue` 调用了不存在的 `notify.error()`/`notify.success()`。在 `useNotify.ts` 中添加了 `error()`/`success()`/`info()` 快捷方法（内部委托 `triggerNotify`），并将 `triggerNotify` 改为 `function` 声明以避免 hoisting 问题。

- **bg-stock-up/down 颜色缺失**：`ChangeBadge.vue` 使用了 `bg-stock-up`/`bg-stock-down` Tailwind 类，但 `style.css` 的 `@theme` 中未定义两个 token。添加 `--color-stock-up: #cf1c1c`（涨=红）、`--color-stock-down: #1a9c1a`（跌=绿），遵循 A 股红涨绿跌惯例。

- **AxSelect `ControlSize` 缺 `xl`**：`SEARCH_SIZE_FIXED` 和 `SEARCH_SIZE_FLEX` 两个 Record 缺少 `xl` key → 补全。

- **ComponentsView `notificationHistory.length`**：`notificationHistory` 是 Ref，模板中访问 `.length` 需 `.value.length`。

- **tsconfig `useDefineForExpose`**：移除无效编译选项。

- **CSS 类型声明**：创建 `src/env.d.ts`，声明 `*.css` 和 `*.vue` 模块类型，解决 TS2882 侧效导入声明缺失。

## 可拖拽分割面板 AxSplitPane (2026-06-03 17:40)

- 创建 `src/components/ui/split/AxSplitPane.vue`：通用可拖拽分割组件
  - 支持 `direction="horizontal"|"vertical"` 双向分割
  - `defaultSize`/`minSize`/`maxSize`：控制固定面板的尺寸约束
  - `minSizeSecond`：弹性面板的最小尺寸保护
  - `reverse` prop：反转受控方向（默认 #first 固定，#second 弹性；reverse 后反置）
  - 使用 Pointer Events 实现拖拽交互，全局 `ax-split-dragging` class 禁止文本选择
  - 分割线悬停/拖拽时高亮为 primary 色（黑），6px 热区 + 2px 视觉线
- 重构 `TradeLayout.vue`：用 AxSplitPane 嵌套替换原 CSS Grid 3列2行布局
  - 6个 AxSplitPane 嵌套实现全部面板可拖拽调整大小：
    1. 工具栏高度 (48px, min 40, max 80)
    2. 自选股宽度 (280px, min 150, max 450)
    3. 右侧面板宽度 (320px, min 220, max 600)
    4. 盘口/下单高度 (260px, min 100)
    5. 底部面板高度 (200px, min 100, max 500) — 使用 reverse
    6. 持仓宽度 (280px, min 150)
- 添加到全局 style.css：`body.ax-split-dragging * { user-select: none !important; }`
- vue-tsc 类型检查通过，vite build 成功

## 表格列宽拖拽 — TanStack Table (2026-06-03 17:47)

- 安装 `@tanstack/vue-table@8.21.3`（--legacy-peer-deps 绕过 echarts 版本冲突）
- 创建 `src/composables/useColumnResize.ts`：封装 `useStorage` 实现列宽 localStorage 持久化
- 重写 `PositionPanel.vue`：用 `useVueTable` + `createColumnHelper` 替换原生 `<table>`，支持 6 列拖拽（股票/持仓/成本/现价/盈亏/市值），通过 `columnResizeMode: 'onChange'` 实时响应
- 重写 `TradeLogPanel.vue`：同样替换为 TanStack Table，支持 6 列拖拽（时间/股票/类型/价格/数量/金额）
- 自定义单元格用 `h()` 渲染现有组件（ChangeBadge、买入/卖出标签），保持 Tailwind 样式不变
- resize handle 用 4px 右侧绝对定位竖条，hover/拖拽中显示 outline-variant 色
- 两个表格的列宽分别存储到 `stock-sim-position-cols` 和 `stock-sim-tradelog-cols` localStorage key
- vite build 成功，无类型错误

## 修复持仓表格数据不显示 (2026-06-03 17:55)

- **根因**：`@tanstack/vue-table@8.21.3` 的 `useVueTable` 源码中使用 `shallowRef(initialOptions.data)`（line 84），当 `data` 是 `ComputedRef` 时，`shallowRef` 不会展开内部 ref，导致 `dataRef.value` 始终是 Ref 对象而非数组，watch 永不触发
- **修复**：两个面板均改为传普通数组 + 手动 `watch` + `table.setState()` 同步数据变化
  - `PositionPanel.vue`：`data: [...trading.positionList]` + watch positionList → table.setState
  - `TradeLogPanel.vue`：`data: [...recentRecords.value]` + watch tradeRecords → ref → table.setState
- vite build 通过

## 修复 TradeLogPanel table undefined 崩溃 (2026-06-03 18:05)

- **根因**：`useColumnSizing` 的 `useStorage` 可能从 localStorage 读到损坏数据（`null`），导致 `columnSizing.value.time` 抛 TypeError，`useVueTable` 未完成初始化，`table` 变量未赋值
- **修复**：`useColumnResize.ts` 改为 `computed({ get, set })` 模式：getter 强制校验值类型，`null`/非对象/数组 → 返回 `{ ...defaultSizes }`；缺失的 key 自动从 defaults 补齐

## AxSplitPane 调试修复 (2026-06-03 18:07)
- **修复 reverse 拖拽方向**：reverse 模式下 `splitPos` 用 `startSize + delta * (-1)`。原因：handle 在 flex 布局中位于两个面板之间，非 reverse 时 #first 随 handle 同向变化（+delta），reverse 时 #second 在 handle 另一侧需反向变化（-delta）
- **修复 HMR 编译错误**：TradeLogPanel.vue 和 PositionPanel.vue 中 `table` 变量在 SFC 中与 HTML `<table>` 元素冲突 → 重命名为 `tradeTable` / `posTable`
- **修复初始拖拽不可用**：handle 的负 margin 被容器 overflow:hidden 裁剪 → 去掉负 margin
## 正确使用 TanStack Table 响应式 data (2026-06-03 18:16)

- **教训**：之前一直用错误模式（plain array + table.setState + flush:sync），这全是 workaround
- **官方正确用法**（v8.20.0+）：
  1. `data` 必须传 `ref()` 或 `computed()`，不能是 `.value` 或普通数组
  2. 更新数据时替换整个 `.value`（`data.value = [...]`），不能 push/splice
  3. 内部 `watchEffect` + `unref(data)` 自动处理响应式，不需要手动 `table.setState()`
- **PositionPanel**：用 `storeToRefs(trading)` 获取原始 ComputedRef，传给 `data: positionList`
- **TradeLogPanel**：创建 `ref<TradeRecord[]>(...)` 作为 data，watch store 变化时替换 `.value`
- 删除了所有之前的 workaround（table.setState、flush:sync、plain array spread）

## 分割面板最终方案：flex + absolute splitter (2026-06-03 18:36)

- **放弃 AxSplitPane 组件**：自定义组件不断出问题（负 margin 裁剪、反向拖拽、knob 居中、圆形/长方形反复改）
- **放弃 splitpanes 库**：引入后 UI 完全变形，嵌套 Flex 与 splitpanes 内部样式冲突
- **最终方案**：纯 flex 布局 + 5 个 absolute 定位的 2px 分割线
  - flex-column：toolbar(48px) | flex-row(填满) | bottom(200px)
  - flex-row：left(280px) | center(flex:1) | right(320px)
  - 5 个 splitter：toolbar下、left右、right左、bottom上、bottomLeft右
  - 拖拽用 PointerEvent，`bottom`/`right` 面板因 CSS `calc(100% - size)` 定位需 delta 取反（`startSize + delta * -1`）
  - 2px 宽/高，默认 outline-variant 灰色，hover/拖拽变黑，cursor 自动切换 col-resize / row-resize

## K线历史 + 天数响应式修复 (2026-06-03 18:48)

- **问题**：图表只有一根K线（空荡荡），天数不刷新
- **MockDataSource 重写**：
  - `initStockStates` 预生成 100 天历史K线（`HISTORY_DAYS = 100`），随机漫步逐日生成 OHLC
  - 恢复原 `tick()` 为**日内微调**（randomWalk 更新 currentPrice/OHLC/minuteData，并实时更新当天 K 线的 close/high/low）
  - 新增 `nextDay()` 方法：保存当天收盘到上一根 K 线，在新日期追加新 K 线
  - `manualRefresh()` 调用 `nextDay()` → 点击刷新按钮 = 推进一天
  - `simDay` getter 改为 `klineCache.length`（所有股票同步，一般为第一个股票的缓存长度）
- **Market Store**：
  - `simDay` 从 `computed` 改为 `ref<number>`，初始值从 dataSource 读取
  - `manualRefresh()` 和 `resetSimulation()` 末尾同步 `simDay.value = dataSource.value.simDay`
  - 这样 Vue 响应式能正确追踪变化

## K线缺口 + 自动刷新图表 bug (2026-06-03 18:58)

- **bug#1**：手动刷新后K线成横线（open=close=high=low）。原因：`nextDay()` 设所有值为开盘价，等 tick 更新，但关闭 auto-refresh 时 tick 永不触发。修复：`nextDay()` 直接生成完整 OHLC（volatility 0.03 随机），不等 tick。
- **bug#2**：自动刷新不更新图表。原因：tick 更新 klineCache 但 ChartPanel 只 watch simDay（tick 不改 simDay）。修复：marketStore 新增 `chartTick` ref，subscribe 回调 `chartTick.value++`；ChartPanel watch 加 `marketStore.chartTick`。
- 成交量恢复 5M-15M（与最初 getKline 一致）

## 数据补全 + gridjs 表格替换 (2026-06-03 19:35)

- **TradeLogPanel 补全列**：新增 编号/成交日期/证券名称/手续费 列，买卖类型显示带颜色，成交日期从 time 字段拆分
- **Toolbar 右上角补全资产数据**：参考市值、盈亏(+金额 +百分比)、持仓比例，与示意图对齐
- **PositionPanel 优化**：表头改为"证券名称/数量/成本价/现价/浮动盈亏/盈亏比/市值"，底部增加总市值+总盈亏汇总栏
- **Trading Store 修复**：
  - `placeOrder` 时 `name` 从 `order.code` 改为正确获取股票名称（优先 order.name → 持仓 name → code fallback）
  - 买入追加持仓后同步更新 marketValue/profit/profitPercent
  - 卖出后 marketValue 按 currentPrice 计算（原按 avgCost 错误）
- **gridjs 替换原生 table**：
  - 安装 `gridjs` + `gridjs-vue`
  - 发现 `gridjs-vue` 源码使用 `Vue.extend`（Vue 2 API），本项目 Vue 3.5.35 不兼容
  - 改用 gridjs 原生 JS API 封装 `GridTable.vue`：支持 columns/rows/resizable props，数据变化时 `updateConfig().forceRender()`
  - 覆盖 `.gridjs-th`/`.gridjs-td` padding 为 `4px 8px`，让表格更紧凑
  - PositionPanel 和 TradeLogPanel 均替换为 GridTable，删除原 `useDragColumnResize` 和 `useColumnResize` 相关代码
- **TradeLayout 类型修复**：`startSize` 可能 undefined → 加类型断言 + guard clause

## 修复：刷新行情不应重置委托数量 (2026-06-03 19:58)

- **问题**：点击刷新按钮或自动刷新时，OrderPanel 的"委托数量（手）"被重置为 1
- **根因**：`OrderPanel.vue` 的 `watch(selectedQuote)` 中，每次行情推送（`market.ts` 用新 `Map` + 新 `StockQuote` 对象替换）都会触发 `quantity.value = 1`
- **修复**：watch 回调中比较新旧股票的 `code`，仅当切换股票时才重置数量；同一只股票行情刷新时保留用户已输入/选择的数量
- **改动**：`src/components/order/OrderPanel.vue` 第 117-123 行

## 持仓面板行点击联动自选股三联动 (2026-06-03 20:05)

- **需求**：点击持仓明细面板某行 → 联动选中左侧自选股 → 右侧交易面板同步更新，实现三处联动
- **实现**：
  - `GridTable.vue`：增加 `selectedId` prop 和 `rowClick` emit；gridjs `className.tr` 根据 `selectedId` 高亮选中行（`bg-primary-container`），所有行加 `cursor-pointer`；`grid.on('rowClick')` 触发 emit
  - `PositionPanel.vue`：
    - rows 中增加 `id: p.code`（gridjs 用做行 ID，不显示）
    - 引入 `useWatchlistStore`，监听 `@rowClick` 调用 `watchlistStore.selectStock(code)`
    - 传 `:selected-id="watchlistStore.selectedStock"` 保持高亮与当前选中股票同步
  - `OrderPanel` 无需改动：卖出时的 `maxSellQty`/`canAffordSell`/`selectedQuote` 本来就基于 `watchlistStore.selectedStock`
- **TypeScript 绕过**：gridjs 的 `className.tr` 类型定义为 `string` 不接受函数，用 `as any` 强转（运行时完全支持函数返回值）

## 修复：持仓面板点击高亮 + 三联动 (2026-06-03 20:16)

- **问题**：
  1. 点击持仓面板行没有高亮
  2. 点击时表格闪烁
  3. 其他两个面板（自选股、交易面板）没有同步选中
- **根因**：
  1. `gridjs` 的 `Row` 对象没有 `id` 属性，`row.id` 返回 `undefined`，emit 的是 `"undefined"`
  2. `watch(() => props.selectedId, updateGrid)` 每次变化都触发 `forceRender()`，导致表格闪烁
  3. `className.tr` 函数在 `updateConfig().forceRender()` 时不一定被重新调用，高亮不生效
- **修复**（重写 `GridTable.vue`）：
  1. 去掉 `gridjs className.tr` 函数方式的高亮，改用纯 DOM 操作
  2. 去掉 `watch(() => props.selectedId, updateGrid)`，不再因选中变化触发 `forceRender()`
  3. `rowClick` 事件中通过 `e.target.closest('.gridjs-tr')` + `Array.from(trs).indexOf(tr)` 找到数据行索引，再取 `props.rows[index].id`，确保 emit 正确的 code
  4. 点击时直接操作 DOM：移除所有行的 `gridjs-row-selected`，给当前行添加该 class（`background-color: var(--color-surface-container-high)` 灰色背景）
  5. `watch(() => props.selectedId, applyHighlight)`：只同步 DOM 高亮，不触发表格重渲染
  6. `updateGrid`（数据变化）后通过 `nextTick(applyHighlight)` 恢复高亮

## 快捷键 r 刷新 + 长按连续刷新 + 缩放保持不变 (2026-06-03 20:36)

- **快捷键 r**：Toolbar.vue 用 `useMagicKeys()` 监听 r 键，watch 触发 `market.manualRefresh()`
- **长按连续刷新**：按下时立即刷新 + `setInterval(manualRefresh, 200)`，松手时 `clearInterval`
- **刷新时缩放不变**：KlineChart.vue 新增 `zoomState` ref，监听 ECharts `@datazoom` 事件保存 start/end，computed option 的 `dataZoom` 改用 `zoomState.value.start/end` 替代写死的 `50/100`

## 真实股票数据接入 — westock-data CLI (2026-06-03 20:42)

- **方案**：westock-data 是 WorkBuddy CLI 技能（node 脚本），前端浏览器无法直接调用。在 Vite dev server 中添加 API 中间件桥接
- **实现文件**：
  - `src/api/stockApiPlugin.ts`：Vite 插件，mount 到 `/api/stock` 路径，提供 `/quote`、`/kline`、`/minute` 三个端点，通过 `child_process.execFile` 调用 westock-data CLI（绝对路径：`C:\Users\Administrator\.workbuddy\plugins\marketplaces\cb_teams_marketplace\plugins\finance-data\skills\westock-data\scripts\index.js`），解析 Markdown 表格输出为 JSON
  - `src/data/RealDataSource.ts`：实现 DataSourceAdapter 接口，通过 `fetch('/api/stock/...')` 获取真实数据。轮询模式（5秒间隔），`subscribe()` 首次拉取 + 定时轮询全部已订阅代码
  - `src/stores/market.ts`：支持 `sourceType` 切换（mock/real），`switchDataSource()` 方法清理旧源、创建新源、重新订阅；mock 特有功能（simDay、reset、manualRefresh 推进一天）仅在 isMock() 时可用
  - `src/components/common/Toolbar.vue`：数据源下拉切换改用 `market.switchDataSource()`（原仅改 settings）；真实模式下隐藏模拟天数和重置按钮
  - `src/data/index.ts`：导出 `RealDataSource`
  - `vite.config.ts`：引入 `stockApiPlugin()`
  - `tsconfig.json`：添加 `"types": ["node"]`
  - `package.json`：安装 `@types/node` (devDependencies, --legacy-peer-deps)
- **关键问题修复**：connect 中间件 `use('/api/stock', handler)` 会剥离路径前缀，`req.url` 已不含 `/api/stock`，路径匹配改为 `/quote`/`/kline`/`/minute`
- **验证**：`curl localhost:5180/api/stock/quote?codes=sz000001` 返回平安银行实时行情 {"code":"sz000001","name":"平安银行","price":10.99,...}；K线 API 同样正常

- **修复 execFile cwd 缺失**：CLI 子进程调用 500 → 加上 `cwd: WESTOK_CWD`，CLI 脚本需在自身目录执行才能解析依赖

- **重构为独立后端**：API 逻辑从 `src/api/stockApiPlugin.ts`（Vite 插件）提取到 `server/stockApi.ts`（connect 中间件）+ `server/index.ts`（独立 HTTP 服务）
  - `npm run dev` → Vite 内置 API 中间件（开发模式）
  - `npm run dev:server` → 独立后端端口 3180
  - `npm run dev:all` → 前后端同时启动
  - 删除 `src/api/` 目录，server 代码与前端代码分离
  - 安装 `tsx` (devDependencies) 用于运行 TypeScript 后端
  - `tsconfig.json` 排除 `server/` 目录；`server/tsconfig.json` 独立配置

## 修复真实数据源：冗余请求 + K线更新 + 加载状态 (2026-06-03 21:41)

- **关闭刷新后仍有大量请求**：3 处修复
  1. `initQuoteSubscription` 仅 mock 模式逐股拉取（real 已由 subscribe 内 `fetchAndPush` 批量处理），消除 12 个冗余 `getQuote`
  2. `switchDataSource` 末尾检查 `autoRefresh`，若关闭立即 `pause()`
  3. `RealDataSource.subscribe()` 加 `if (!this._paused)` 守卫，暂停时不再拉取和轮询
- **K 线切换不更新**：kline CLI 参数从 `--period day --limit 120`（flag 在 execFile 中不生效）改为位置参数 `kline <code> day 120`；CLI 倒序 → `.reverse()` 转为正序
- **加载状态**：ChartPanel 新增 loading spinner + 错误提示，loadData 加 try/catch；`:key` 改为 `selectedStock + '-' + klinePeriod`

## 修复真实数据源三个Bug (2026-06-03 21:03)

- **关闭刷新后仍有 HTTP 请求**：DepthPanel.vue 有独立 `setInterval(loadDepth, 3000)` 定时器，不受 autoRefresh 控制。改为 watch `marketStore.chartTick`（跟随主刷新节奏），去掉独立定时器
- **手动刷新后图表不更新**：manualRefresh 对真实数据源逐个 getQuote，每次 chartTick++ 导致 watch 触发 N 次 loadData。改为 `Promise.all` 批量拉取后只递增一次 chartTick；ChartPanel KlineChart 加 `:key` 确保切换股票/周期时强制重建
- **盘口数据一直变化**：DepthPanel 的独立定时器已移除，盘口现在只在切换股票、手动刷新、自动刷新时更新

## 真实数据模拟回放 + 缓存 (2026-06-03 22:33)

- **缓存模块** `src/data/CacheManager.ts`：key 前缀 `stock:quote:{codes}` / `stock:kline:{code}:{period}`，每天 0 点自动过期
- **RealDataSource 重写为模拟回放模式**：
  - `init(codes)`：批量拉取 120 天 K 线 + 行情，缓存到 CacheManager + 内存 store
  - `simDay` 起始 30，`manualRefresh()` → simDay++ → `pushQuotes()` 用当天 K 线 close 更新行情
  - `getKline()` 返回 `klines[0..simDay]`，切换股票按当前 simDay 截取
  - `reset()` → 回到 30 天，与 MockDataSource 接口完全对齐
- **market store**：`switchDataSource('real')` 调用 `realDS.init()`；manualRefresh/reset 统一处理两种模式
- **Toolbar**：移除 `v-if="sourceType==='mock'"`，simDay + 重置在两种模式下都显示